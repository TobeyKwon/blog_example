spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kwon
    password: 1234
    url: jdbc:mysql://localhost:3306/blog?allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul

  data:
    web:
      pageable:
        one-indexed-parameters: true

  jpa:
    hibernate:
      ddl-auto: create
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true
